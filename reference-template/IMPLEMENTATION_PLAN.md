# Implementation Plan

> The living task list — the agent's working memory across context resets.
> Generated/refined in **plan mode**; executed one task per loop in **build mode**.
> Priority uses the **`999+` convention**: higher number = more critical / do-first.

## Status legend

- `[ ]` not started   `[~]` in progress   `[x]` done

## Tasks

- [ ] **1000 — _<task title>_**
  - Why: _<the goal / which spec this serves>_
  - Acceptance: _<mechanical check that proves it's done — a passing test/command>_
- [ ] **1010 — _<task title>_**
  - Why: _<…>_
  - Acceptance: _<…>_
- [ ] **1020 — _<task title>_**
  - Why: _<…>_
  - Acceptance: _<…>_

<!-- Inject urgent work with a higher number, e.g. "9999 — fix broken build",
     so it's picked first without renumbering everything below. -->

## Learnings

<!-- The agent appends durable learnings here: decisions made, dead ends,
     non-obvious facts discovered. This survives context resets. -->

- _<learning>_

## Blocked / Questions

<!-- The agent records blockers and ambiguities here instead of thrashing or
     guessing. A human resolves these between runs. -->

- _<none yet>_
